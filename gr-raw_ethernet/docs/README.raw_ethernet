This is the raw_ethernet-write-a-block package meant as a guide to building
out-of-tree packages. To use the raw_ethernet blocks, the Python namespaces
is in 'raw_ethernet', which is imported as:

    import raw_ethernet

See the Doxygen documentation for details about the blocks available
in this package. A quick listing of the details can be found in Python
after importing by using:

    help(raw_ethernet)
